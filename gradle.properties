# Done to increase the memory available to gradle.
org.gradle.jvmargs=-Xmx1G
org.gradle.parallel=true

# org.gradle.java.home — set per-machine in ~/.gradle/gradle.properties, NOT here.
# CI (GitHub Actions) sets JAVA_HOME via the workflow; local dev uses Scoop JDK.

# IntelliJ IDEA is not yet fully compatible with configuration cache, see: https://github.com/FabricMC/fabric-loom/issues/1349
org.gradle.configuration-cache=false

# Fabric Properties
# check these on https://fabricmc.net/develop
minecraft_version=26.2
loader_version=0.19.5
loom_version=1.17.20
fabric_kotlin_version=1.14.1+kotlin.2.4.20

# Mod Properties
version=1.3.2
group=mod.ex3.companion

# Dependencies
fabric_api_version=0.160.0+26.2

# Dev-only test mods (localRuntime: never compiled against, never bundled)
# Modrinth version id of JEI 30.29.0.201 (Fabric, MC 26.2) — latest release
jei_version=d3HeviSL
# Modrinth version id of Jade 26.2.11+fabric
jade_version=ue8CO97w
# Modrinth version id of Resourcify 1.8.6 (26.2) — resource-pack manager, dev convenience only
resourcify_version=FXpCik8Z
# Modrinth version ids for shader stack (dev convenience only)
sodium_version=xJZxADzI
iris_version=gxZWWnKH
# Modrinth version id of LambDynamicLights 4.12.4 (26.2) — provides Fabric Light API (dynamic entity light)
lambdynlights_version=XVsaQXSm
# Modrinth version id of Mod Menu 20.0.2 (Fabric, MC 26.2)
modmenu_version=WdLLrOzD
# YACL (YetAnotherConfigLib) — config screen library
yacl_version=3.9.6+26.2-fabric
